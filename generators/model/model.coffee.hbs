_             = require 'lodash'
joi           = require 'joi'

class {{addonPrefix}}::{{#camelize}}{{name}}{{/camelize}} extends FoxxMC::Model
  @Module: {{addonPrefix}}
  @attr 'title',        joi.string().empty(null).empty('').default(null)
  @attr 'description',  joi.string().empty(null).empty('').default(null)


module.exports = {{addonPrefix}}::{{#camelize}}{{name}}{{/camelize}}.initialize()
