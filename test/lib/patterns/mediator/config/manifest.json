{
  "name": "test",
  "version": "1.0.0",
  "configuration": {
    "port": {
      "description": "port for test switch server",
      "type": "number",
      "default": 3000
    },
    "trustProxy": {
      "description": "trustProxy for test switch server",
      "type": "boolean",
      "default": false
    },
    "subdomainOffset": {
      "description": "subdomainOffset for test switch server",
      "type": "number",
      "default": 2
    }
  }
}
